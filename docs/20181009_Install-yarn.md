
## Yarn install (on mac)

- https://yarnpkg.com/lang/ja/docs/install/#mac-stable

```bash
$ brew install yarn
```

更新は `$ brew upgrade yarn`

## Usage

- `yarn add <package>` : option --save
- `yarn remove <package>`
- `yarn global <cmd>`
